# 数据库驱动：mysql（生产，推荐）| sqljs（本地零依赖 WASM SQLite）
DB_DRIVER=mysql

# MySQL 数据库连接配置（DB_DRIVER=mysql 时生效）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_DATABASE=risk_admin

# JWT 密钥（生产环境务必修改）
JWT_SECRET=risk-admin-secret-change-me

# 服务端口
PORT=3000
